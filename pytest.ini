[pytest]
addopts = -m "not llm and not integration"
testpaths = tests
junit_suite_name = cognitive_staffing
markers =
    llm: marks tests that require live LLM connectivity and remain opt-in via -m llm (LLM_MARKER)
    integration: marks tests that require external services or a full Streamlit runtime
