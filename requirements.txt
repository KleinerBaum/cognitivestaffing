# requirements.txt

# Core dependencies
streamlit>=1.33
streamlit-sortables==0.3.1
openai>=1.99.3  # responses stream event compatibility
pydantic>=2.5,<3.0
email-validator>=2.1
PyYAML>=6.0
jsonschema>=4.18
backoff>=2.2
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp==1.25.0

# Core ingestion & parsing
beautifulsoup4>=4.12
chardet>=5.0
httpx>=0.24
langdetect>=1.0
lxml>=4.9
pycountry>=23.12
pypdf>=4.0

# Application utilities
python-docx>=0.8.11
python-dotenv>=1.0
requests>=2.31
types-requests
types-PyYAML
plotly>=5.20

# Developer tooling (install with `pip install -r requirements.txt` for a unified stack)
pytest>=8.0
mypy>=1.10
ruff>=0.5

# Optional extras (OCR, spaCy, converters) live in requirements-optional.txt
